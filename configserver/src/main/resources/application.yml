spring:
  application:
    name: "configserver"
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/thantsoun/action-management-config.git"
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true

#encrypt:
#  key: "1234567890"

server:
  port: 8123
